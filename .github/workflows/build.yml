name: Gerar APK SignalPlay TV

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    name: Compilar APK
    runs-on: ubuntu-latest

    steps:
      - name: Baixar Código do Repositório
        uses: actions/checkout@v4

      - name: Configurar Java JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Instalar Gradle Compatível
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.2'

      - name: Construir o APK (Release)
        run: gradle assembleRelease

      # =========================================================================
      # ASSINATURA FINAL: Formato PKCS#12 exige apenas UMA senha!
      # =========================================================================
      - name: Assinar APK Oficial (Modo PKCS12)
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        run: |
          # 1. Limpa o Base64 e salva com a extensão correta (p12)
          echo "$KEYSTORE_BASE64" | tr -d '\n\r ' | base64 --decode > blindagem.p12
          
          # 2. Pega a ferramenta Android mais atualizada
          BUILD_TOOLS=$(ls -d /usr/local/lib/android/sdk/build-tools/* | tail -1)
          
          # 3. Alinha a memória do APK
          $BUILD_TOOLS/zipalign -v -p 4 app/build/outputs/apk/release/app-release-unsigned.apk app-release-aligned.apk
          
          # 4. Assina informando que é PKCS12 e usando APENAS a senha do cofre!
          $BUILD_TOOLS/apksigner sign --ks blindagem.p12 --ks-type pkcs12 --ks-key-alias "key0" --ks-pass env:KEYSTORE_PASSWORD --out SignalPlay-TV-Oficial.apk app-release-aligned.apk

      - name: Disponibilizar APK Assinado para Download
        uses: actions/upload-artifact@v4
        with:
          name: SignalPlay-TV-Oficial-Assinado
          path: SignalPlay-TV-Oficial.apk
