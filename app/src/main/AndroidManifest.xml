<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tv.signalplay">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />

        <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/banner_tv"
        android:label="SignalPlay" />
        
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.SignalPlay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="false" android:theme="@style/Theme.SignalPlay" />
        <activity android:name=".BuscaActivity" android:exported="false" android:theme="@style/Theme.SignalPlay" />
        <activity android:name=".CanaisActivity" android:exported="false" android:theme="@style/Theme.SignalPlay" />
        <activity android:name=".DetalhesActivity" android:exported="false" android:theme="@style/Theme.SignalPlay" />
        <activity android:name=".PlayerActivity" android:exported="false" android:theme="@style/Theme.SignalPlay" />
        
   <activity android:name="com.tv.signalplay.EpgActivity" android:exported="false" android:theme="@style/Theme.SignalPlay" />


    </application>

</manifest>
