plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
    // Aplica o plugin do Google Services
    id 'com.google.gms.google-services'
}

android {
    namespace 'com.signalplay.tv'
    compileSdk 34

    defaultConfig {
        applicationId "com.signalplay.tv"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
}

dependencies {
    // Importa a BOM do Firebase
    implementation platform('com.google.firebase:firebase-bom:32.7.0')
    
    // Adiciona o motor do banco de dados Firestore
    implementation 'com.google.firebase:firebase-firestore'

    // ESSA É A LINHA QUE FALTAVA: Adiciona o motor do RecyclerView (Carrossel de Filmes)
    implementation 'androidx.recyclerview:recyclerview:1.3.2'
}
