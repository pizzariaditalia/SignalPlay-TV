plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    // Plugin necessário para o Firebase funcionar
    id("com.google.gms.google-services") 
}

android {
    namespace = "com.signalplay.tv"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.signalplay.tv"
        // Compatibilidade máxima com TV Boxes antigas (Android 5.0+)
        minSdk = 21 
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            // MÁGICA 1: Ativa a criptografia do código (ProGuard/R8)
            isMinifyEnabled = true 
            
            // MÁGICA 2: Remove arquivos e recursos não utilizados para deixar o APK mais leve
            isShrinkResources = true 
            
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
        }
        debug {
            // Durante os testes rápidos, mantemos o código normal para compilar mais rápido
            isMinifyEnabled = false
            isShrinkResources = false
        }
    }

    // Mantendo a compatibilidade exigida pelo seu ambiente de compilação no GitHub
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    
    kotlinOptions {
        jvmTarget = "17"
    }
}

dependencies {
    // Essenciais do Android
    implementation("androidx.core:core-ktx:1.12.0")
    implementation("androidx.appcompat:appcompat:1.6.1")
    implementation("com.google.android.material:material:1.11.0")
    implementation("androidx.constraintlayout:constraintlayout:2.1.4")
    
    // Componentes de Layout e Prateleiras
    implementation("androidx.recyclerview:recyclerview:1.3.2")
    implementation("androidx.cardview:cardview:1.0.0") // Necessário para a sombra 3D do Top 10

    // Firebase (Banco de Dados em Tempo Real para Histórico e Favoritos)
    platform("com.google.firebase:firebase-bom:32.7.1")
    implementation("com.google.firebase:firebase-firestore-ktx")

    // OkHttp (Motor de comunicação de alta velocidade com o servidor IPTV)
    implementation("com.squareup.okhttp3:okhttp:4.12.0")

    // Coroutines (Processamento em Segundo Plano para não travar a tela)
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3")

    // Glide (Carregamento e Caching das Capas dos Filmes)
    implementation("com.github.bumptech.glide:glide:4.16.0")
    annotationProcessor("com.github.bumptech.glide:compiler:4.16.0")

    // Media3 / ExoPlayer (O verdadeiro motor de vídeo Premium do aplicativo)
    implementation("androidx.media3:media3-exoplayer:1.2.1")
    implementation("androidx.media3:media3-ui:1.2.1")
    implementation("androidx.media3:media3-exoplayer-hls:1.2.1")
}
